package UI;

public enum ID {
   ActionButton,
   Text,
   Checkbox,
   TextField,
   ToggleButton,
   ColorGradient,
   Slider,
   HoverDetails,
   ImageBox,
   DropdownList,
   Item;

   // $FF: synthetic method
   private static ID[] $values() {
      return new ID[]{ActionButton, Text, Checkbox, TextField, ToggleButton, ColorGradient, Slider, HoverDetails, ImageBox, DropdownList, Item};
   }
}
